version: '3.7'
services:
  backend:
    build: 
        context: .
        dockerfile: ./docker/api/Dockerfile.api
    tty: true
    volumes:
    - .:/app
    depends_on:
      - mysql
    ports:
      - 8080:8080
    stdin_open: true
    env_file: .env
  front:
    build: 
        context: .
        dockerfile: ./docker/front/Dockerfile.front
    tty: true
    volumes:
    - .:/app
    depends_on:
      - backend
    ports:
      - 3000:3000
  mysql:
    image: mysql:5.7
    restart: always
    ports:
      - 3306:3306
    volumes:
      - ./backend/db/mysql/my.cnf:/etc/mysql/conf.d/my.cnf
      - ./backend/db/mysql_data:/var/lib/mysql
    env_file: .env.dev.db